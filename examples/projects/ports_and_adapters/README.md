# Notification ports and adapters

The workflow depends on a narrow delivery protocol. Composition selects an email adapter; the domain and the protocol do not import infrastructure.

The source root is `.`. This project is input to a static analyser; its modules are never imported by the corpus tests. See the central manifest for the expected structural outcome.
