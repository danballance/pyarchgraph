# Cycle diluted by 49 independent pairs

A mathematical control: 49 unrelated one-way pairs push the headline above 98, but the original two-module cycle still needs a finding. Files are deterministic committed inputs, not created during test collection.

The source root is `.`. This project is input to a static analyser; its modules are never imported by the corpus tests. See the central manifest for the expected structural outcome.
