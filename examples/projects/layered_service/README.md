# Layered order service

A presentation layer delegates through an application service to persistence and a shared domain model. A long acyclic path is legitimate; a 90-point score threshold would reject this simple design.

The source root is `.`. This project is input to a static analyser; its modules are never imported by the corpus tests. See the central manifest for the expected structural outcome.
